const validation = require("./validation");
const validationParams = require('./validationParams');
const authenticate = require("./authenticate");
const upload = require("./upload");
const validationVerify = require("./validationVerify");

module.exports = {
  validation,
  validationParams,
  authenticate,
  upload,
  validationVerify,
};
